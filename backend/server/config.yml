server:
  host: 0.0.0.0
  port: 8888
#mysql
datasource:
  driver: mysql
  host: localhost
  port: 3306
  database: scm
  username: root
  password: root123456
  config:
    max_conn: 100
    min_conn: 5
    connect_timeout: 5
    acquire_timeout: 5
    max_lifetime: 100
    idle_timeout: 100
    sqlx_level: info
  migration: fresh

cache:
  driver: redis
  host: 0.0.0.0
  port: 6379
  password:
logger:
  level: info
jwt:
  secret: "your-256-bit-secret-key-change-in-production"
  expire_hours: 24
